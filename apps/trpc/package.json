{
  "name": "trpc",
  "type": "module",
  "scripts": {
    "build": "bun run check:type && tsdown",
    "check:type": "tsc",
    "dev": "bun run --hot src/app.ts",
    "start": "bun run dev"
  },
  "dependencies": {
    "db": "workspace:*",
    "hono": "^4.9.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "tsdown": "^0.15.6",
    "typescript": "catalog:"
  }
}
